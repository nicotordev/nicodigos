# Servicio aparte del web: ejecuta un POST al cron de la app Next.js.
# En Railway: Settings → Root Directory = /services/cron-sync-catalog

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
cronSchedule = "*/15 * * * *"
restartPolicyType = "NEVER"
